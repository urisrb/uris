---
title: Overview
description: What uris catalogs, where it reads from, and the two interfaces to it.
---

uris catalogs files and records that already live somewhere else. You attach the places they live,
such as buckets, directories, mailboxes, calendars, feeds, wikis, and repositories. uris reads each
one and builds a single index over all of them. You can search that index, ask questions of it, and
export from it. The bytes stay in their original places. uris stores where each thing lives and
what its analysis found.

Two interfaces reach the same catalog. The browser app uses [GraphQL](/reference/graphql/), and
the [MCP endpoint](/concepts/agents/) serves a model. Both accept the same access token, which
[masks](https://github.com/masksrb/masks) issues. Each interface calls the domain layer directly,
and neither is built on the other.

## Key concepts

| | |
| --- | --- |
| [Feeds](/concepts/feeds/) | the record for each file, note, address, tag, and content type, and the edges between them |
| [References](/concepts/references/) | where a feed's bytes live, and how uris notices a change to them |
| [Adding things](/concepts/adding/) | uploads, notes, fetched pages, snapshots, and syncs, and how uris chooses where a file is stored |
| [Analysis](/concepts/analysis/) | how uris reads a feed: the steps, the summary, and the agent that files it |
| [Resources](/concepts/resources/) | the places uris reads from, writes to, and sends requests to |
| [Search](/concepts/search/) | the index, and how lexical and semantic results are combined |
| [Agents and MCP](/concepts/agents/) | the four tools, and the agent loop that calls them |
| [Tenants](/concepts/tenants/) | isolation between tenants, and signing in through masks |

Start with the [quickstart](/quickstart/). The reference pages are generated from the code, so they
match the running version.
