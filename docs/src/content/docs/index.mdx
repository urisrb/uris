---
title: thingies
description: Everything you make, see, send and keep — one searchable index, and a way back out.
template: splash
hero:
  tagline: everything you make, see, send and keep — one index, one way out
  actions:
    - text: Run it
      link: /start/running/
      icon: document
    - text: Drive it from Claude
      link: /guides/connector/
      icon: right-arrow
      variant: minimal
---

import { Card, CardGrid, LinkCard } from "@astrojs/starlight/components";

Your things, together. A Rails application that catalogues your storage accounts, mailboxes and
calendars, analyzes what it finds, indexes it in OpenSearch, and serves the result over MCP and
GraphQL.

<CardGrid stagger>
  <Card title="A thing is a reference" icon="seti:db">
    One `things` row, plus a `thing_references` row per place the bytes live. Originals are never
    copied into the catalog.
  </Card>
  <Card title="Types are STI subclasses" icon="puzzle">
    `Resource::S3`, `Resource::Imap`, `Resource::OauthGoogle` and six more, each owning its adapter,
    command schema, locator shape and page enumerator.
  </Card>
  <Card title="The grant builds the tool list" icon="seti:config">
    `POST /mcp`, eleven tools, four scopes. `Grant#tools` filters `Tool.all` per request, so a tool
    outside the token's scopes is absent from `tools/list`.
  </Card>
  <Card title="Bulk work resumes" icon="approve-check">
    Sync, export, analyze, reindex and dedupe run through
    [job-iteration](https://github.com/Shopify/job-iteration) and checkpoint at a cursor.
  </Card>
</CardGrid>

<CardGrid>
  <LinkCard title="Running it" href="/start/running/" description="brew bundle, bin/setup, bin/dev." />
  <LinkCard title="The four movements" href="/start/movements/" description="sync, analyze, search, export." />
  <LinkCard title="Things" href="/concepts/things/" description="Columns, kinds, children, deletion." />
  <LinkCard title="Resources" href="/concepts/resources/" description="Nine types, their capabilities, and which can sync." />
  <LinkCard title="Tenancy" href="/concepts/tenancy/" description="Five isolation layers and the tests that assert them." />
  <LinkCard title="MCP tools" href="/reference/tools/" description="All eleven, with scopes and arguments." />
</CardGrid>
