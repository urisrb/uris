- name: Create the things deploy directory
  ansible.builtin.file:
    path: "{{ things_deploy_path }}"
    state: directory
    mode: "0750"

- name: Template the things compose file
  ansible.builtin.template:
    src: compose.yml.j2
    dest: "{{ things_deploy_path }}/compose.yml"
    mode: "0640"

- name: Pull the things images
  ansible.builtin.command:
    cmd: docker compose pull
    chdir: "{{ things_deploy_path }}"
  changed_when: false

- name: Start things
  ansible.builtin.command:
    cmd: docker compose up -d
    chdir: "{{ things_deploy_path }}"
  register: things_up
  changed_when: "'Started' in things_up.stderr or 'Recreated' in things_up.stderr or 'Created' in things_up.stderr"

- name: Wait for things to answer its health check
  ansible.builtin.command:
    cmd: docker compose exec -T things curl -fsS http://localhost/up
    chdir: "{{ things_deploy_path }}"
  register: things_health
  until: things_health.rc == 0
  retries: 60
  delay: 5
  changed_when: false
